# Diego Castro dotfiles

## install

Run this:

```sh
git clone --recursive https://github.com/diegocastro/dotfiles.git ~/dotfiles
cd ~/dotfiles
script/install
```

@TODO Add submodule to install script
@TODO Use make to install
